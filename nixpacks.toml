[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm run install:all"
]

[phases.build]
cmds = [
  "npm run build",
  "rm -rf public && cp -r frontend/dist public"
]

[start]
cmd = "npm start"
